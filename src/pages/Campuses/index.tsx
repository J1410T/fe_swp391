import * as React from 'react';

/**
 * Campuses management page component
 * Allows managing information about addresses, descriptions, and images of FPT campuses
 */
export function Campuses(): React.ReactElement {
  return (
    <div>
      <h1>Cơ sở đào tạo</h1>
    </div>
  );
}

export default Campuses;

import { loginRoute } from './login';

/**
 * Auth routes configuration
 * Chứa các route xác thực, hiện tại chỉ có route đăng nhập
 */
export const authRoutes = [
  loginRoute
];

export default authRoutes;
